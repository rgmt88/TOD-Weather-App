import { getCurrentWeather, getForecast } from "./fetchData.js";

async function getNextSixDayForecast(location) {
    try {
        
    } catch (error) {
        console.error('Error: ', error);
    }
}